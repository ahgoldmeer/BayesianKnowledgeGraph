remote_theme: pages-themes/slate@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
title: Bayesian Knowledge Graph
description: A small academic project site for the Bayesian Knowledge Graph capstone.
show_downloads: true

defaults:
  - scope:
      path: ""
    values:
      layout: default
